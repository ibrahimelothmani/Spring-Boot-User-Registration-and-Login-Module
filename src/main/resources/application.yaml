spring:
  datasource:
    url: jdbc:mysql://localhost:3306/login_system
    username: root
    #   password: Mysql@123 we don't use password for local development
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
   properties:
    hibernate:
    dialect: org.hibernate.dialect.MySQLDialect
    ddl-auto: update
    show-sql: true